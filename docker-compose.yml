services:
  postgres:
    image: postgres:16
    container_name: trading_platform_postgres
    environment:
      POSTGRES_DB: trading_platform_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
